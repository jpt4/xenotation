;;xeno.mk
;;20240101Z
;;jpt4
;;Chez Scheme v9.5.8
;;bidirectional xenotation transliterator using miniKanren
;;arabic <-> {tic, nullified} xenotation

#|
(define (xeno a t n)
  (conde
    implexo
|#

;given a prime, return its index
;(define (implexo p i)

;is prime?

;(define (primo n)

(define (factoro n f)
  (fresh (q)
    (/o n f q '())))

(define (factors-aux n f ls)
  (

(define (factors n fls) 
  (fresh (max)
	 (sqrto n max)
	 (factors-aux n max fls)))
  
(define (sqrto n s)
  (* s s sp)
  (conde
   [(== sp n)]
   [(minuso sp n m) (< m n)]))
